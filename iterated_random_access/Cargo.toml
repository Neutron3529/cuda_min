[package]
name = "iterated_random_access"
version = "0.1.0"
edition = "2024"
publish = ["crates-io"]

[dependencies]
cuda_min = { path = "../cuda_min" }
rayon = "1.10.0"
[build-dependencies]
cuda_min = { path = "../cuda_min" }
